#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'VRP with VROOM tests.',
        'data' => [ ],
        'tests' => [qw(
            doc-vrp_vroom
            )],
        'documentation' => [qw(
            doc-vrp_vroom
            )]
    },

);

1;
