#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'test for or-tools',
        'data' => [],
        'newdb' => 1,
        'tests' => [qw(
            knapsack
            multiple_knapsack
            bin_packing
            )],

        'documentation' => [qw(
            knapsack
            multiple_knapsack
            bin_packing
            )],

        'nottesting' => [qw(
            )]
    },
);

1;